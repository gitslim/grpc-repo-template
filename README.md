# Template for maintaining Protobuf files.

* store .proto files in git
* compile .proto files with protoc, generate grpc-stubs for java and clojure
* build jar artifact
* deploy to private maven repo